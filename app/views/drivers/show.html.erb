<h1><%= current_user.name %></h1>

<% if current_user.current_ride %>

  <%= render "shared/current_ride" %>

<% else %>

  <h2>Available Rides</h2>

  <hr>

  <% active_rides(current_user).each do |ride| %>

    <h4>Pickup Location</h4>
    <%= ride.pickup %>

    <h4>Dropoff Location</h4>
    <%= ride.dropoff %>

    <h4>Number of Passengers</h4>
    <%= ride.passenger_number %>
    <br>
    <%= link_to "Accept Ride", ride, method: "put" %>

    <hr>
  <% end %>
<% end %>

<%= render "shared/completed_rides" %>
